/**
 * Re-export the shared VibesMarkdownRenderer for backward compatibility.
 * The actual implementation lives in @/components/ui/VibesMarkdownRenderer.
 */
export { DocsMarkdownRenderer, VibesMarkdownRenderer } from "@/components/ui/VibesMarkdownRenderer";
